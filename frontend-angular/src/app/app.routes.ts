import { Routes } from '@angular/router';

/**
 * Tabla de enrutamiento principal de la aplicación (Application Routing Tree).
 * Centraliza la navegación implementando un contrato estricto de Type safety enforcement.
 * Diseñado bajo el principio Open/Closed: preparado para la inyección de rutas 
 * mediante Lazy Loading y Abstraction layering.
 */
export const routes: Routes = [];